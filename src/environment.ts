export default {
    debug: true,
    testing: true,
    apiEndpoint: "https://api.communityedits.com/graphql",

    /**
     * 0 -> No logging
     * 1 -> Errors only
     * 2 -> +warnings
     * 3 -> +info
     * 4 -> +debug
     */
    logLevel: 4
};
